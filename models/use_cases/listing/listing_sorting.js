function sortByState(listings) {
    let activeListings = [];
    let otherListings = [];
}

function sortByExpiration(listings) {

}

function sortListings(listings) {
    listings = sortByState(listings)
}

module.exports = {
    sortListings: sortListings
};